apiVersion: v1
kind: Service
metadata:
  name: {{ include "msdemo-cartservice-chart.fullname" . }}
  labels:
    {{- include "msdemo-cartservice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "msdemo-cartservice-chart.selectorLabels" . | nindent 4 }}
